import { createCss } from '@stitches/react';

export const {
  css,
  theme,
  styled,
  global,
  keyframes,
  getCssString,
} = createCss({
  theme: {},
  media: {},
  utils: {},
  prefix: '',
  insertionMethod: 'append | prepend | () => {}',
  themeMap: {},
});
